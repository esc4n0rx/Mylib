# FFmpeg binaries

Place the Windows executables here:

```text
tools/ffmpeg/ffmpeg.exe
tools/ffmpeg/ffprobe.exe
```

These paths are the MyLib defaults. They can be overridden with
`MYLIB_FFMPEG_PATH` and `MYLIB_FFPROBE_PATH`.

The executables are intentionally not committed to the repository.
